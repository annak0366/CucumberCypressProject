name: Autotesting

on:
  push:
    branches:
      - 'main'

jobs:
  tests:
    runs-on: ubuntu-latest
    
    steps:
      - name: Install Dependencies
        run: npm ci
                
      - name: Run Tests
        run: npm test